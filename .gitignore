__pycache__/
*.py[cod]
*$py.class
*.log
*.bak
*.bak.*
.env
venv/
env/
.venv/
.pytest_cache/
test.db
market_cache.json
openclaw.json
node_modules/
.DS_Store

# Keep local data and model logs out of tracking
*.db
*.db.old
*.jsonl
.claude/

# RL artifacts (large runtime files)
rl_training_data.jsonl
rl_policy_model.pkl
training/rl_sft_dataset/

# Pipeline artifacts (large runtime files)
rl_models/

# LoRA training checkpoints (multi-GB; recreated each training run)
training/lora_checkpoints/

# Old / backup model snapshots
*.pkl.OLD_*

# Broker installer tarballs (download from official site, don't commit)
moomoo_OpenD_*.tar.gz
ibgateway*.sh

# Per-environment secrets (loaded by start.sh via `source backend/.env`)
backend/.env

# Runtime-generated catalyst attribution stats (auto-updated by RL pipeline)
intelligence_attribution_report.json

# Kronos model weights (425MB; download per README, don't commit)
kronos_lib/

# Runtime state (regenerated by the engine)
.broker_capabilities.json
.position_highs.json
